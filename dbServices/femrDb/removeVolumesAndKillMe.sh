#This would be a file you could run to just kill this and remove its volumes.
#This is more for development, for when you want to just kill and remove all
#trace of this container.

#Heads up, i don't quite remember if this can be easily killed by script, you might
#need to manually get volume and container ids to kill them.